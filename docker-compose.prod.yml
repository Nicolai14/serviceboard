services:
  nginx:
    ports:
      - "127.0.0.1:8085:80"
    restart: always

  app:
    restart: always
    environment:
      - PHP_OPCACHE_VALIDATE_TIMESTAMPS=0

  mysql:
    restart: always

  redis:
    restart: always
